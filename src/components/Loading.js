import { ThreeDots } from "react-loader-spinner";

export default function Loading () {
    return (
        <ThreeDots
            height="20"
            width="20"
            radius="6"
            color="white"
            ariaLabel="loading"
            // wrapperStyle
            // wrapperClass
        />);
}